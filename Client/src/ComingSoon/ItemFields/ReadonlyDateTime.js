import React from "react";
import { Input } from "semantic-ui-react";

const ReadonlyDateTime = () => (
  <Input placeholder="itemid" type="datetime-local" readonly />
);

export default ReadonlyDateTime;
